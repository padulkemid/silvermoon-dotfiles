#:schema https://docs.jj-vcs.dev/latest/config-schema.json

[user]
name = 'Fadhil Muhammad'
email = 'bringitdown0@gmail.com'
username = 'padulkemid'
# key = input your key here


# split in jj
[signing]
behavior = 'own'
backend = 'gpg'
# machine-local, set in the untracked ~/.config/jj/conf.d/local.toml
# key = '<key>'

[git]
executable-path = '/opt/homebrew/bin/git'

[ui]
pager = ['delta', 'delta', '--pager', 'less -FRX']
diff-formatter = ':git'
diff-editor = ":builtin"
default-command = "log"
